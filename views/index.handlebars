<h1>Burgers!</h1>

<h2>Burgers that have not been eaten!</h2>

<ul>
  {{#each burger}}
    {{#unless devour}}
      {{> burger/burgers-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been eaten!</h2>

<ul>
  {{#each burger}}
    {{#if devour}}
      {{> burger/burgers-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="bur" name="name">
  </div>

  <div class="form-group">
    <label for="devour">devour?</label><br>
    <input type="radio" name="devour" value="1" checked> Eat!<br>
    <input type="radio" name="devour" value="0"> not eating that
  </div>

  <button type="submit">Add Burger</button>
</form>
